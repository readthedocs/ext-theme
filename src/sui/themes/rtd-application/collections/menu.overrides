// A helper from grid to hide some of the top navigation menu items as the
// viewport narrows
@media only screen and (max-width: @largestTabletScreen) {
  .ui.ui.ui.menu > .computer.only.item,
  .ui.ui.ui.menu > .menu > .computer.only.item {
    display: none;
  }
}

.dark-theme-override({
  .ui.attached.menu,
  .ui.vertical.menu {
    &:not(.borderless):not(.text) {
      border: 1px solid rgba(255, 255, 255, 0.15);
    }
  }
});

// Same as the dropdown override: disabled items with a popup keep pointer
// events so the tooltip can show.
.ui.ui.menu .item.disabled[data-content] {
  pointer-events: auto;
}
